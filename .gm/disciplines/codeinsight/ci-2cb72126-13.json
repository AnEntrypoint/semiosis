core/test_manifold_invariants.py:180:183 test_kb_exact_text_top_hit
def test_kb_exact_text_top_hit() -> None:
    kb = _kb()
    hits = kb.search("gamma separate idea", k=1)
    assert hits and hits[0].text == "gamma separate idea"