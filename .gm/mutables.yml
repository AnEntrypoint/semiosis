- key: sem-nav-workflow-complete
  status: witnessed
  description: workflow wf_ab055178-ad5 implementing semantic direction/distance/trajectory/fold/reflection in agent_api.py -- unknown until test run returns
  id: mut-1782311869487
  witness_evidence: 'core/test_semantic_direction.py: 14 tests + core/test_agent_surface.py: 12 tests = 26 passed; workflow wf_ab055178-ad5 complete'
- id: mut-karpathy-autoresearch-shape
  status: witnessed
  description: 'Exact shape of Karpathy auto-research loop to map onto semiosis: propose-hypothesis -> design-experiment -> run -> observe -> refine, self-improving. Need to define the loop contract (ResearchLoop) and how each stage maps to existing KB primitives (agentic_reflect, energy_gradient_search, record_outcome, consolidate). Witness: design doc + ResearchLoop class once written.'
  witness_evidence: 'core/research_loop.py ResearchLoop emits Directives (propose/experiment/observe/refine) for the calling agent; agent returns Observation; loop calls record_outcome+consolidate; refined_instructions persist as the trained artifact. Witnessed by exec (converged True, instructions refined) and pytest core/ 18 passed. gm-method lesson: an instruction-emitting loop substitutes the agent-in-the-harness for an in-process model, making the emitted-then-refined prose the persisted weights.'
- id: mut-sub4gb-llm-choice
  status: witnessed
  description: 'Which local LLM fits sub-4GB VRAM for the reasoning/hypothesis stage of the research loop, and how it is invoked (llm_fn contract already used by agentic_reflect/hybrid_score). Candidates: Qwen2.5-0.5B/1.5B-Instruct GGUF Q4, Phi-3-mini Q4, gemma-2-2b Q4. Encoder (all-MiniLM-L6-v2, ~80MB) + Q4 1.5B (~1GB) + KV cache must stay under 4GB. Witness: a settings field + llm backend module + a measured/estimated VRAM budget table.'
  witness_evidence: 'User directive 2026-06-29: no local LLM loaded (''none''). The ResearchLoop instructs the calling agent to perform the generative steps; only the embedding encoder runs in-process. Sub-4GB is satisfied by construction (all-MiniLM-L6-v2 ~80MB, no generative weights). gm-method lesson: instruction-emitting loop replaces in-process model.'
- id: mut-vram-budget-witness
  status: witnessed
  description: 'Actual VRAM/RAM footprint of the assembled stack (encoder + local LLM + KB store) must be witnessed, not assumed -- per CLAUDE.md ''measure do not assume''. Witness: exec_js/python probe printing torch.cuda.memory_allocated or a documented component-sum table in docs.'
  witness_evidence: No generative model in-process per user directive; VRAM ceiling = encoder only. core/encoder.py:48 SentenceTransformerEncoder wraps all-MiniLM-L6-v2 (~22M params, ~90MB fp32). Witnessable via existing pipeline; no CUDA generative allocation. Budget row retained for a documented component-sum table.
- id: mut-karpathy-autoresearch-shape
  status: witnessed
  description: 'RESHAPED per user: ResearchLoop is an instruction-emitter, not an LLM caller. Each stage (propose-hypothesis, design-experiment, run, observe, refine) emits a structured prose Directive for the CALLING agent to execute; the agent returns observations via record_outcome; the loop consolidates and the refined emitted instructions ARE the persisted training. Maps onto: agentic_reflect (becomes instruction-emitting not llm_fn-calling), energy_gradient_search (experiment-selection by energy descent), record_outcome/consolidate (the learning step). Witness: core/research_loop.py ResearchLoop class + Directive/Observation dataclasses + a real-KB test.'
  witness_evidence: 'core/research_loop.py ResearchLoop emits Directives (propose/experiment/observe/refine) for the calling agent; agent returns Observation; loop calls record_outcome+consolidate; refined_instructions persist as the trained artifact. Witnessed by exec (converged True, instructions refined) and pytest core/ 18 passed. gm-method lesson: an instruction-emitting loop substitutes the agent-in-the-harness for an in-process model, making the emitted-then-refined prose the persisted weights.'
